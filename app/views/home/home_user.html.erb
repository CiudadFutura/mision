<div class="center-section">
  <% flash.each do |key, value| %>
    <div class="<%= flash_class(key) %>">
      <%= value %>
    </div>
  <% end %>
  <div class="row">
    <h3>Hola <%= current_usuario.nombre %></h3>
  </div>
  <br>
  <br>
  <% if !current_usuario.completed? or current_usuario.circulo.blank? %>
    <div class="row">
      <div class="col-sm-12">
        <div class="alert alert-info">
          <h4>Para poder realizar tus pedidos debés</h4>
          <ul>
            <% if !current_usuario.completed? %>
              <li>Completar tus datos personales haciendo clic en
                <span>
                    <%= link_to(edit_usuario_path(current_usuario)) do %>
                      Mis Datos
                    <% end %>
                  </span>
              </li>
            <% end %>
            <% if current_usuario.circulo.blank? %>
              <li>Pertenecer a un círculo de asociades,
                <span>
                    <%= link_to(usuario_path(current_usuario)) do %>
                      Mi Círculo
                    <% end %>
                  </span>
              </li>
            <% end %>
            <li>
              Para más información sobre como realizar un pedido
              <%= link_to(como_compro_path(1)) do %>
                Como comprar
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </div>
  <% end %>
  <br>
  <div class="row">
    <div class="col-sm-12">
      <p>Si estas acá, es porque sos coordinador de un círculo en la Misión Anti Inflación.<br/>
        Desde tu panel, ademas de hacer tu pedido, vas a poder administrar los integrantes<br/>
        de tu círculo y realizar el pago de la compra. Ante cualquier duda, consultanos vía mail
        a <b>victoriacolectiva@gmail.com</b>
      </p>
    </div>
  </div>

  <% if @ciclo_actual_completo.present? %>
    <% if @ciclo_actual_completo.circulos.include?(current_usuario.circulo) %>
      <div class="row">
        <h4>Ciclo actual</h4>
        <div class="col-sm-12">
          <p class="text-center">Ciclo Actual: <%= @ciclo_actual_completo.nombre %></p>
          <p>Desde el <strong><%= l @ciclo_actual_completo.fecha_inicio_compras %></strong></p>
          <p>hasta el <strong><%= l @ciclo_actual_completo.fecha_fin_compras %></strong></p>
          <p>Ultimo día para realizar el pago <strong><%= l @ciclo_actual_completo.fecha_fin_pagos %></strong></p>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="error text-center">No hay Ciclo Activo</div>
  <% end %>
  <div class="row">
    <% if @compra.present? %>
      <div id="ciclos" data-start-date="<%= @compra.first.fecha_inicio_compras %>"></div>
      <div class="row calendar text-center">
        <div class="col-md-6 col-md-offset-3">
          <h1>Próximo Ciclo de compras de tú círculo</h1>
          <div id="calendar"></div>
        </div>
      </div>
    <% end %>
  </div>


</div>





