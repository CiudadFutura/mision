<ul class="nav nav-pills nav-stacked left-menu" id="stacked-menu">
  <li><%= link_to 'Categorías', productos_path(:categoria_id => 'Todas'), class: 'navbar-link' %></li>
	<% i = 1
		 cat = ''
	%>
  <% @categorias_menu.each do |c| -%>
		<% if c[:nombre] == 'Frutas y Verduras'
			cat = 'frutasyverduras'
    elsif c[:nombre] == 'Higiene personal'
			cat = 'higienepersonal'
    elsif c[:nombre] == 'Canastas'
      cat = 'canastas'
    end %>
      <% if c[:subcategorias].present? %>
          <li class="dropdown">
            <a href="#" data-target="#item<%=i%>" data-toggle="collapse" data-parent="#stacked-menu"><%= c[:nombre] %><span class="glyphicon icon-<%=c[:nombre].downcase %> pull-left"></span></a>
            <ul class="nav nav-stacked collapse left-submenu" id="item<%=i%>">
              <% c[:subcategorias].each do |sm| %>
                <li><%= link_to sm[:nombre], productos_path(categoria_id: c[:id], subcategoria_id: sm[:id])%></li>
              <% end %>
            </ul>
          </li>
      <% i += 1 %>
      <%else %>
          <li>
            <%= link_to(productos_path(categoria_id: c[:id]), class: 'navbar-link') do %>
              <span class="glyphicon icon-<%=cat %> pull-left"></span><%= c[:nombre]%>
            <% end %>

      <% end %>
  <% end -%>
</ul>


