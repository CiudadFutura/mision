<nav class="navbar navbar-default">
  <div class="container">
    <ul class="nav navbar-nav navbar-left">
      <% if can? [:manage], categorias_path %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Menu <i class="fa fa-chevron-down"></i></a>
          <ul class="dropdown-menu" role="menu">
            <% if can? [:manage], circulos_path %>
              <li>
                <%= link_to(circulos_path) do %>
                  <span class="icon-cogs"></span>Circulos
                <% end %>
              </li>
            <% end %>
            <% if can? [:manage], categorias_path %>
              <li>
                <%= link_to(categorias_path) do %>
                  <span class="icon-key2"></span>Categorías
                <% end %>
              </li>
            <% end %>
            <% if can? [:manage], compras_path %>
              <li>
                <%= link_to(compras_path) do %>
                  <span class="icon-key2"></span>Ciclos
                <% end %>
              </li>
            <% end %>
            <% if can? [:manage], suppliers_path %>
              <li>
                <%= link_to(suppliers_path) do %>
                  <span class="icon-key2"></span>Proveedores
                <% end %>
              </li>
            <% end %>
            <% if can? [:manage], usuarios_path %>
              <li>
                <%= link_to(usuarios_path) do %>
                  <span class="icon-key2"></span>Usuarios
                <% end %>
              </li>
            <% end %>
            <% if can? [:manage], geo_reports_path %>
              <li>
                <%= link_to(geo_reports_path) do %>
                  <span class="icon-stats"></span>Mapa Reporte
                <% end %>
              </li>
            <% end %>
            <% if can? [:manage], reports_path %>
              <li>
                <%= link_to(reports_path) do %>
                  <span class="icon-stats"></span>Productos por Prov.
                <% end %>
              </li>
            <% end %>
            <% if can? [:manage], pedidos_path %>
              <li>
                <%= link_to(remitos_pedido_index_path) do %>
                  <span class="icon-stats"></span>Remitos
                <% end %>
              </li>
            <% end %>
          </ul>
        </li>
      <% end %>

      <% if usuario_signed_in? %>
        <li>
          <% if can? [:manage], pedidos_path %>
            <%= link_to(pedidos_path) do %>
              <span class="icon-stats"></span>Pedidos
            <% end %>
          <% else %>
              <%= link_to(pedidos_path) do %>
                <span class="icon-stats"></span>Mis Pedidos
              <% end %>
          <% end %>
        </li>
      <% end %>
      <% if usuario_signed_in? %>
        <li>
          <% if can? [:manage], accounts_path %>
            <%= link_to(accounts_path) do %>
              <span class="icon-stats"></span>Cuentas Corriente
            <% end %>
          <% else %>
            <%= link_to(account_path) do %>
              <span class="icon-stats"></span>Mi Cuenta Corriente
            <% end %>
          <% end %>
        </li>
      <% end %>

    </ul>

    <ul class="nav navbar-nav navbar-right">
      <% if usuario_signed_in? %>
        <li>
          <%= link_to(productos_path) do %>
            <span class="icon-tienda glyphicon"></span>Tienda
          <% end %>
        </li>
        <li><p class="navbar-text"><%= truncate(current_usuario.email, length: 32) %></p></li>
        <li class="divider"></li>
        <li><%= link_to('Mis Datos', usuario_path(current_usuario))%></li>
        <li class="divider"></li>
        <li><%= link_to('Cerrar Sesion',
                        destroy_usuario_session_path,
                        method: 'delete') %>
        </li>
      <% else %>
        <li>
          <%= link_to(new_usuario_registration_path) do %>
            <span class="icon-registrate glyphicon"></span>Registrate
          <% end %>
        </li>
        <li>
          <%= link_to(new_usuario_session_path) do %>
            <span class="icon-ingresa glyphicon"></span>Ingresa
          <% end %>
        </li>
        <li>
          <%= link_to(productos_path) do %>
            <span class="icon-tienda glyphicon"></span>Tienda
          <% end %>
        </li>
      <% end %>
    </ul>
    <%= yield(:cart_menu) %>
  </div><!-- /.container-fluid -->
</nav>
